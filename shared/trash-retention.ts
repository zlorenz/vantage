/**
 * Shared trash retention — used by Studio Content tool and Next purge cron.
 * Keep dependency-free so both TypeScript projects can import it cleanly.
 */

export const TRASH_RETENTION_DAYS = 30
