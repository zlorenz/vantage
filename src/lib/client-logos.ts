/**
 * Client logo registry — re-export from shared (Studio + Next).
 */
export {
  CLIENT_LOGOS,
  CLIENT_LOGO_BY_ID,
  HOME_BRAND_LOGO_IDS,
  getClientLogo,
  getClientLogos,
  isClientLogoId,
  type ClientLogo,
  type ClientLogoId,
} from '@client-logos'
